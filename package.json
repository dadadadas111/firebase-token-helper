{
  "name": "firebase-token-helper",
  "version": "0.1.0",
  "description": "CLI helper to generate Firebase ID tokens for a given UID using Admin SDK custom tokens and REST auth exchange",
  "main": "index.js",
  "bin": {
    "firebase-token-helper": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "lint": "echo 'no linter configured'"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "firebase-admin": "^11.10.0",
    "node-fetch": "^3.3.1",
    "yargs": "^17.7.2"
  }
}
